<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8">
  <title>Shot Me Down</title>
  <style>
    body { background: #111; color: #f0f0f0; font-family: Arial, sans-serif; }
    a { color: #0ff; }
    .shot { display: inline-block; padding: 4px; margin: 2px; border: 1px solid #444; width: 80px; text-align: center; }
    .complete { background: #0a0; }
    nav a { margin-right: 10px; }
  </style>
</head>
<body>
  <nav>
    <% if (user) { %>
      <a href="/dashboard">Dashboard</a>
      <a href="/stats">Stats</a>
      <a href="/leaderboard">Leaderboard</a>
      <a href="/logout">Logout</a>
    <% } else { %>
      <a href="/login">Login</a>
      <a href="/register">Register</a>
    <% } %>
  </nav>
  <hr>
